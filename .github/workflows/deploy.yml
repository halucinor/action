name : push and deploy

# Event
on:
  push:
    branches: [ "main" ]	

# Job
jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: connect and git pull
        uses: appleboy/ssh-action@master
        with:
          host : 112.169.122.210
          username : ${{ secrets.NAME }}
          password : ${{ secrets.PASSWORD }}
          port : ${{ secrets.PORT }}
          script: |
            cd workspace/action
            echo ${{ secrets.HOST }}
            git pull
